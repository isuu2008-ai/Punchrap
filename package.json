{
  "name": "punchlab",
  "version": "0.1.0",
  "private": true,
  "description": "Local rap vocal recorder and render studio prototype.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node scripts/check.mjs",
    "desktop:check": "node scripts/check-desktop-contract.mjs",
    "desktop:doctor": "node scripts/desktop-doctor.mjs",
    "desktop:serve": "node scripts/start-desktop-dev-server.mjs",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.3"
  }
}
